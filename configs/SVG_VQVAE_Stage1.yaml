model_params:
  name: "SVG_VAQVAE"  # always keep this "VectorGPT"
  vector_decoder_model: "mlp" # "mlp" or "raster_conv"
  quantized_dim: 512
  codebook_size: 4096  # will be ignored for FSQ
  image_loss: "mse"  # "pyramid" or "mse"
  single_code_representation: true
  vq_method: "fsq"  # "vqvae", "FSQ", "vqtorch"
  fsq_levels: [7,5,5,5,5]  # will determine codebook_size, see Table 1 of FSQ paper - [7,5,5,5,5] for 4096 (e.g. StrokeNUWA), [8,5,5,5] for 1024

data_params:
  dataset: "stage1"  # must be in the DATASETMAP in run.py
  top_level_dir: "/scratch2/moritz_data/glyphazzn/svgs_simplified"  # root path for CausalSVGDataModule in dataset.py
  channels: 3  # use 3
  width: 128
  train_batch_size: 8
  val_batch_size:  8
  num_workers: 8
  individual_max_length : 7.5  # max length of a single stroke, decides the width/height of rendered shapes
  stroke_width: 0.7  # IMPORTANT: this must scale with individiual_max_length as they together define the image
  subset: "all"
  # max_shapes_per_svg: 32

exp_params:
  lr: 0.00002  # 0.00002
  weight_decay: 1.e-4  # specify positive float to enable, start experimenting with 1.e-4/1.e-3
  scheduler_gamma: 0.98  # 0.95 is a good starting value
  train_log_interval: 100 # len(dataset) / train_batch_size / desired_logging_frequency
  manual_seed: 1265

trainer_params:
  devices: -1  # always keep at -1 as this takes all available GPUs specified through CUDA_VISIBLE_DEVICES
  max_epochs: 250  # dsnt matter too much, got early stopping implemented
  # accumulate_grad_batches: 2

logging_params:
  # entity: "aiis-chair"  # comment to use default wandb entity "mfeuer"
  project: "thesis"  # your wandb project name
  save_dir: "/scratch2/moritz_logs/SVG_VQVAE/Stage1/glyphazzn_full_single_code"
  name: "SVG_VQVAE glyphazzn Stage1, single code representation"  # name of the run in wandb
  version: 0
  author: "Moritz"  # will be a tag in wandb